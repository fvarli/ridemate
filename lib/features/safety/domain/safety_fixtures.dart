// ─────────────────────────────────────────────────────────────
// RideMate — Safety fixture
//
// Transcribed from the approved design. Read safety_snapshot.dart first.
// ─────────────────────────────────────────────────────────────

import 'safety_snapshot.dart';

/// The Safety Center exactly as the design draws it.
const SafetySnapshot kMockSafetySnapshot = SafetySnapshot(
  trustedContactCount: 2,
);
